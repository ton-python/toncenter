---
title: "Get Decode"
openapi: "GET /api/v3/decode"
---

## Parameters

| Parameter | Required | Description                                                   |
| --------- | -------- | ------------------------------------------------------------- |
| `opcodes` | No       | List of opcodes to decode (hex or decimal).                   |
| `bodies`  | No       | List of message bodies to decode (base64 or hex).             |

## Response

Returns [`DecodeResponse`](/api_v3/models#decoderesponse).

| Field     | Type                              | Description                    |
| --------- | --------------------------------- | ------------------------------ |
| `opcodes` | `list[str] \| None`               | Decoded opcode names.          |
| `bodies`  | `list[dict[str, Any]] \| None`    | Decoded message bodies.        |

## SDK Usage

```python
import asyncio

from toncenter.rest import ToncenterRestClient
from toncenter.types import Network

OPCODES = ["0x0f8a7ea5"]


async def main() -> None:
    async with ToncenterRestClient(network=Network.MAINNET) as client:
        result = await client.v3.utils.get_decode(
            opcodes=OPCODES,
        )
        print(result.model_dump_json(indent=4))


if __name__ == "__main__":
    asyncio.run(main())
```
