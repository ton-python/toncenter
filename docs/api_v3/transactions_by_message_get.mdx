---
title: "Get Transactions By Message"
openapi: "GET /api/v3/transactionsByMessage"
---

## Parameters

| Parameter   | Required | Description                                                 |
| ----------- | -------- | ----------------------------------------------------------- |
| `msg_hash`  | No       | Message hash (hex, base64, base64url).                      |
| `body_hash` | No       | Hash of message body.                                       |
| `opcode`    | No       | Opcode in hex or signed 32-bit decimal form.                |
| `direction` | No       | Direction of message.                                       |
| `limit`     | No       | Max rows to return (default 10).                            |
| `offset`    | No       | Skip first N rows (default 0).                              |

## Response

Returns [`TransactionsResponse`](/api_v3/models#transactionsresponse).

| Field          | Type                            | Description                    |
| -------------- | ------------------------------- | ------------------------------ |
| `transactions` | `list[Transaction] \| None`     | Matching transactions.         |
| `address_book` | `AddressBook \| None`           | Address book entries.          |

## SDK Usage

```python
import asyncio

from toncenter.rest import ToncenterRestClient
from toncenter.types import Network

MSG_HASH = "4eee803beab5a31685b5c06a0716bd770f662db0395acbebc2ee6dd9c8227c34"


async def main() -> None:
    async with ToncenterRestClient(network=Network.MAINNET) as client:
        result = await client.v3.blockchain.get_transactions_by_message(
            msg_hash=MSG_HASH,
        )
        print(result.model_dump_json(indent=4))


if __name__ == "__main__":
    asyncio.run(main())
```
